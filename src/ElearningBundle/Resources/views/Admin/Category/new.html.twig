{% extends '::base.html.twig' %}

{% block body %}
    <h1>Category creation</h1>
    {{ form_start(form, {'action': path('category_create'), 'method': 'POST'}) }}
    {{ form_widget(form) }}

    <input type="submit" value="Create"
           class="btn btn-default" />
    {{ form_end(form) }}
    <a href="{{ path('category') }}" class="btn btn-default">
        Back to the list
    </a>
{% endblock %}
